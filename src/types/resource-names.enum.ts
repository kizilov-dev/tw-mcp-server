export enum ResourceNames {
  ALLOWED_PRESETS = "allowed_presets",
  VCS_PROVIDERS = "vcs_providers",
  VCS_PROVIDER_REPOSITORIES = "vcs_provider_repositories",
  DEPLOY_SETTINGS = "deploy_settings",
}
